{
  "name": "connectingbee",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npx concurrently \"npm run start --prefix backend\" \"npm run preview --prefix frontend -- --port 3000\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}